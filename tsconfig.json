{
    "extends": "@react-native/typescript-config/tsconfig.json",
    "compilerOptions": {
        "baseUrl": ".",
        // 基础路径，一般为项目根目录
        "paths": {
            "@/*": [
                "src/*"
            ]
            // 将 '@' 指向 'src' 目录
        }
    }
}
